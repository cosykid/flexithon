# Claude API key — enables real AI photo verification + council letter drafting.
# Without it, CurbCut runs in demo mode (mock verification, template letters).
ANTHROPIC_API_KEY=

# Optional overrides
# ANTHROPIC_MODEL=claude-opus-4-8
# PORT=4141
# CURBCUT_AI=mock   # force demo mode even when a key is present
