# Flutter
app/.dart_tool/
app/.flutter-plugins*
app/.packages
app/build/
app/.idea/
app/*.iml

# Platform folders are generated by `flutter create .` — commit them once created
# (kept out until then so the repo stays clean pre-generation)

# Supabase
supabase/.temp/
supabase/.branches/

# Secrets
*.env
.env*

# Local launch script with real keys
app/run.sh

node_modules/

# Maps API keys — real values, never commit
app/ios/Flutter/Secrets.xcconfig
